// import { RootState } from "../store/store";
// import { IInvestmentData } from "../models/contracts/IInvestmentData";
// import { useSelector } from "react-redux";

// const useAssets = () => {
//   const properties: IInvestmentData = useSelector(
//     (state: RootState) => state.assests.properties,
//   );
//   const crypto: IInvestmentData = useSelector(
//     (state: RootState) => state.assests.crypto,
//   );
//   const stocks: IInvestmentData = useSelector(
//     (state: RootState) => state.assests.stocks,
//   );
//   const rareMetals: IInvestmentData = useSelector(
//     (state: RootState) => state.assests.rareMetals,
//   );

//   return [properties, crypto, stocks, rareMetals];
// };

// export default useAssets;
